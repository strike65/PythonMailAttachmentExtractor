# Core dependencies (all part of standard library)
# No external dependencies required for basic functionality

# Optional dependencies
colorama>=0.4.4  # For Windows color support (optional)

# Development dependencies
pytest>=7.0.0
pytest-cov>=3.0.0
black>=22.0.0
pylint>=2.15.0
mypy>=0.990
flake8>=5.0.0